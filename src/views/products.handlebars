
<div style="display: flex">
    <h1> Hello, Welcome {{user.first_name}} </h1><a href='/carts{{user.cart}}'><button> CARRITO </button></a>
    <h2> <a href="/api/users/premium"> Configuración de cuenta</a></h2>
    <div>
        CHAT
        <input id="chatBox">
        <p id="messageLogs"> </p>
    </div>

</div>
    <h2>Products list</h2>
    <div class="product-list">
        <div class="product-card">
        <input type="hidden" id="cartId" value="{{user.cartsId}}">
        {{#each product}}
            <h2>{{this.title}}</h2>
            <p>Description: {{this.description}}</p>
            <p>Price: {{this.price}}</p>
            <label for="quantityProduct_{{this._id}}"></label>
            <input id="quantityProduct_{{this._id}}" name="quantity" style="width: 30px;" type="number"> 
            <button productId={{this._id}}>Agregar al Carrito</button>
        {{/each}}

        </div>
    </div>
    
    <div class="pagination">
        </br>
        <a href="/api/sessions/logout"><button> Logout </button></a>
        </br>
        </br>

        {{#if hasPrevPage}}
            <a href="?page={{prevPage}}">Previous</a>
        {{/if}}
        {{#if hasNextPage}}
            <a href="?page={{nextPage}}">Next</a>
        {{/if}}
    </div>
<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/socket.io/socket.io.js"> </script>
<script src="/js/index.js"></script>
<script src="/js/products.js"></script>
<script src="/js/premium.js"></script>
